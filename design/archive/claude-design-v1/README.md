# Claude Design v1 archive

> ⚠️ Historical source archive. Do not use it as the current full-layout target.

These four files are the v1 source files that were later overwritten in place by
the v2 bundle. They were restored byte-for-byte from commit
`5ab8ad17f824e80b4245c94e76ea67e0f466c330` so the v1 design remains directly
inspectable without relying on an external service or Git archaeology.

The v1 rendered references live in `design/reference/`:

- `kaleid-final.png`
- `tokens-dark-mid.png`
- `tokens-light.png`
- `canvas-overview.png`

The external v1 Claude Design projects were last fetched on 2026-05-22 and are
now unavailable. Canonical/current scope and the one approved v1 layout
exception are defined in `docs/handover/design-map.md`.
