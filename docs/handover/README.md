# Handover 入口（本分支为指针）
🔴 **本项目的 handover 正本在 `dev` 分支**，本文件只是指路牌，不含内容。

    git fetch origin dev && git checkout dev

然后读 `docs/handover/README.md`（读序在其 §2）。

规范正本：`github.com/ByAnDa/bylaws`（本机固定位置 `~/repos/bylaws`，没有就 clone）。

最后更新：2026-07-24 by @kaleidLead
